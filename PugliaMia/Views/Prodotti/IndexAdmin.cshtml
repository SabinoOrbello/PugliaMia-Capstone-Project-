@model IEnumerable<PugliaMia.Models.Prodotti>

@{
    ViewBag.Title = "IndexAdmin";
}

<h2>IndexAdmin</h2>

<p class="pt-4">
    @Html.ActionLink("Create New", "Create")
</p>
<div class="container pt-4">
    <h2>Prodotti</h2>
    <div class="row row-cols-1 row-cols-md-3 g-4">

        @foreach (var item in Model)
        {
            <div class="col">
                <div class="card border-0 bg-dark" style="width: 18rem;">
                    <img src="@Url.Content(string.IsNullOrEmpty(item.Immagine) ? "~/Content/Images/hotel.jpg" : item.Immagine)" class="card-img-top" alt="...">
                    <div class="card-body">
                        <h5 class="card-title">@Html.DisplayFor(modelItem => item.Nome)</h5>
                        <p class="card-text">@Html.DisplayFor(modelItem => item.Prezzo)</p>
                      

                        <div>
                            @Html.ActionLink("Edit", "Edit", new { id = item.ProdottoID }) |
                            @Html.ActionLink("Details", "Details", new { id = item.ProdottoID }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.ProdottoID })
                        </div>
                    </div>
                </div>
            </div>
        }

    </div>
</div>


